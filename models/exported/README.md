# Exported Models

Exported embedded and host inference models will live here.

Expected future artifacts:

- TensorFlow Lite Micro compatible local model
- Host fallback model
- Model metadata describing input shape, labels, and confidence semantics

