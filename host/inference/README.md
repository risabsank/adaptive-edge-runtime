# Host Inference

Host-side fallback inference code will live here.

Phase 1 exposes a simple fallback endpoint from `host/controller/app.py`. This directory is reserved for richer model loading, inference backends, and fallback service separation.

